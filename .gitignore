# Mac stuff, ignore
.DS_Store
# Eclipse stuff, should be for each developer, so, ignore
.classpath
.project
.settings
# since we use Maven, ant buil.xml will be private for each developers
#  (if they need their own scripts to do some stuff)
build.xml
# maven target (we definitely do not want this one in git)
target/
# prevent tmp/ should be for dev db and stuff
tmp/

# Ignore .py files as they are dev-ops stuff
.py

# Ignore Intellij files
.idea/
*.iml

# Ignore Sublime files
*.sublime-project
*.sublime-workspace